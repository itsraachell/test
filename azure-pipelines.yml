trigger:
- main

pool:
  vmImage: windows-latest

steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '3.x'
- script: pip install selenium html-testRunner
  displayName: 'Install dependencies'

- script: python -m unittest test/focoenobra/QA_firmaDigital.py
  displayName: 'Run tests'
